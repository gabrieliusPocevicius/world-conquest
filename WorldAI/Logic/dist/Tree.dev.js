/* class Family{
    constructor(parent, child) {
         this.parent = parent = {parent:parent, chidren:child.length};
    }
}

let family = new Family('John', ['Evan', 'Johnson','James']);
console.log(family);  


 */

/* let map = [0, 0, 0, 0, 0, 0];


let fight;
let counter = 0;
let play = setInterval(() => {
    ++counter;
    fight = Math.round(Math.random() * map.length);
    let currentPlace = map.fill("O", 0, fight);
    
    console.log(counter," ", currentPlace);
    if(currentPlace[currentPlace.length - 1] === 'O'){
        clearInterval(play);
    }
},100); */

/* let person = {
    fullName: (city, country)=>{
        return this.firstName + ' ' + this.LastName + ', ' + city + ',' + country;
    }
} 

process.stdout.write(person.fullName);

var person1 = {
    firstName:'John',
    lastName:'Doe'
};
 */
"use strict";